import 'package:flutter/material.dart';

class ListaWidget extends StatelessWidget {
  const ListaWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("T"),),);
  }
}
